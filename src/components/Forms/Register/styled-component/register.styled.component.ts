import styled from "styled-components";

export const StyledRegister = styled.form`
  color: black;
  padding: 2rem;
  background-color: white;
  display: flex;
  flex-wrap: wrap;
  gap: 20px 0;

  input {
    padding: .75rem;
  }

  .input-email {
    width: 100%;
  }
  .input {
    width: 50%;
  }
`